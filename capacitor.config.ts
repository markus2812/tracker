import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.markus2812.reset',
  appName: 'Reset',
  webDir: 'dist',
  server: {
    cleartext: true,
  },
}

export default config
